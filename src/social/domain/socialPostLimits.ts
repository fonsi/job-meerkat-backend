/** AT Protocol hard limit for app.bsky.feed.post text. */
export const BLUESKY_MAX_GRAPHEMES = 300;

/** Meta Threads API hard limit for the `text` param. */
export const THREADS_MAX_CHARACTERS = 500;
